{
  "name": "hashing-ecdsa-poc-web",
  "version": "1.0.0",
  "private": true,
  "description": "SHA-256 + ECDSA P-256 web PoC — test suite",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFiles": ["./tests/setup.js"]
  }
}
